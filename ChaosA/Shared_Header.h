#pragma once

/* 

Cells {
	states:
		hunger
		running speed
		rushing // rushing skill, forwawrd with fast speed
		direction // running on direction
		perception{
			36 * {
				(0 - max vision) object distance
				(0 / 1 / 2) object [neutral, same species, or different species]
			}
		}

	property
		vision
		size
		speed => max velocity 
		agility => max turing direction angle
		neural network {
			layers{
				[nodes]{
					weights,
					value
				}
			}
		}

}


*/